const transactions = (state = [], action) =>
{
    switch (action.type)
    {
        case 'GET_ALL_HISTORY':
          return state
        default:
          return state
    }
}

export default transactions